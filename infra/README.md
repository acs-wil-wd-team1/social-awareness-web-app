# Stage 3 infrastructure

**Status:** Reserved for Stage 3 — deployment not started

This folder will contain approved, non-secret files used for the Stage 3 cloud IaaS deployment.

Possible contents include:

- Web-server configuration
- Application-service configuration
- Deployment and verification scripts
- Non-secret environment examples
- Health-check and rollback instructions

AWS was recorded as the initial cloud choice at Meeting 1. AWS EC2 remains a proposed IaaS option until the team completes the cloud analysis and confirms the deployment design.

Never store passwords, access keys, SSH private keys or copied server secrets here.
